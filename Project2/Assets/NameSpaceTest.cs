using System.Collections;
using System.Collections.Generic;
using UnityEngine;


//Namespaces are created with the use of the namespace 
//Namespaces are a collection of classes with the similar names, created to help stop clashing of same name 
//functions. 

namespace NameSpacesTest
{

    /// <summary>
    /// Two classes that are both character controllers, but each one was made by a different programmer
    /// Using the namespace, it can help differentiate between the two of them. 
    /// This is useful because it makes it so you don't have to change the name of all the clsses individualy. 
    /// This can help differentiate different controllers from each other, say the player controllers vs the AI controllers. 
    /// </summary>
    /// 
}
